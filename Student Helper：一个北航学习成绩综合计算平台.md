Student Helper：一个北航学习成绩综合计算平台

统一认证登录 -> 获取课程、成绩、学分等

首先支持多种筛选查看各种课程成绩

核心功能：

0. 通过学校接口获得课程、成绩、学分等内容，保存到数据库。这些内容可查看，也可手动修改。可以设置是否自动更新。

1. 综合成绩测算（基础的总计、按学期、按学年等课程加权平均分、算术平均分、gpa这个是最基础的，要有，但是不是核心功能）：对于特定规则下面的成绩计算，用于计算保研成绩、奖学金成绩等。

具体设计：网站是多用户系统，“用户”与统一认证学号绑定，但是不保存统一认证密码，用户可以输入密码登录或失效后重新登录。用户登录后从特定接口读取课程成绩等信息，可以刷新。然后用户可以创建、发布以及管理、使用“计分规则”，即哪些课程计入保研（不一定全部修完了，不同进度下只计算当前修完的课程的分数），规则有一些复杂的项，比如哪几门课计入规则，有些课不看，然后有一部分选修课其中选多少学分，然后选修了的计入成绩，两级制、五级制等课程怎么计入gpa，然后列出平均gpa（加权）、加权平均分、算术平均分，以及规则内已修分数、未修分数，未修课程可以使成绩达到的最高最低成绩范围等等，你多设计一下。

但是其实创建以及修改规则在网页上很麻烦，你可以先设计一下有哪些规则，第一版可以重点不在修改上，先创建一个系统默认规则，启动时就有的，能够完成计算等工作就行。默认计分规则见《软件学院推荐优秀应届本科毕业生免试攻读研究生的平均学分绩点成绩规则说明-2024级（发布版）.pdf》；课程培养方案见 `document.pdf`。需要你探索完成接口之后、设计之后，再结合文档的成绩计算办法去完成规则设计。

2. 后续版本添加：分析报告。按照趋势预测未来分数、可以努力的方向等等。可以调用AI大模型。这点的扩展性可以增强。
3. 后续也可添加：手动修改添加规则很麻烦，可以给ai一个文档，ai根据规则生成，人工可后续调整。
4. 后续也可以添加：综测成绩计算。单独有综测成绩计算规则，有些设计和上面类似，从某个成绩（可以引用上面规则）上面计算加分，加分项目全部手动，加分分不同部分，也可能分不同学期，都可能有各自上下限，加分计算方式可能有总体上下限、加分比例等等，有些可以根据奖项、志愿时长、学生工作等直接计算，不用手动输入。这些内容可以保存数据库，防止重复输入。



总体要求：轻量化、多用户（匹配关系做好，而且单一用户在系统多次登录，只在学校登录一次，除非凭证过期）、数据本地存储（只有密码不存）、规则共享公开、界面简洁现代流畅，不要炫酷界面，清晰，成绩计算多元，多种数据，最好用Python，响应式布局。网页端，两种部署模式：windows一键脚本（包括Python环境，数据持久化）、docker compose服务器部署。



学校接口参考“[BUAASubnet/UBAA: 更好的智慧北航. Make BUAA Great Again!](https://github.com/BUAASubnet/UBAA)”这个项目，可以拉取对应的git到本地，看一下他怎么登录统一认证、获取课程信息、成绩等等。

顺便你也看看有没有获取全班成绩、平均分等数据的接口（我是学校管理员，这是给学生便利的网站，这个可以做），便于增加分析排名等功能。
